@model List<DuAnEnglish.Models.PhongHoc>
@{
    if (Session["Role"] != null)
    {
        var role = Session["Role"].ToString().ToLower();
        if (role == "admin")
        {
            Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
        }
        else if (role == "giangvien")
        {
            Layout = "~/Views/Shared/_LayoutGiangVien.cshtml";
        }
    }
}
<!-- SweetAlert2 -->
<link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.19/dist/sweetalert2.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.19/dist/sweetalert2.all.min.js"></script>


@*<h2>Danh sách Phòng Học</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Mã Phòng Học</th>
            <th>Tên Phòng Học</th>
            <th>Sức Chứa</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var phong in Model)
        {
            <tr>
                <td>@phong.IDPhongHoc</td>
                <td>@phong.TenPhong</td>
                <td>@phong.SucChua</td>
            </tr>
        }
    </tbody>
</table>*@

<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="bg-white p-10 rounded-2xl shadow-lg w-full max-w-6xl">

        <h2 class="text-3xl font-bold text-center text-red-600 mb-6">Danh Sách Phòng Học Của Trung Tâm</h2>

        <div class="text-left mb-6">
            <a href="@Url.Action("Them", "QuanLyPhongHoc")"
               class="inline-block py-2 px-4 text-sm font-medium rounded-lg bg-green-500 text-white hover:bg-green-400">
                + Thêm mới
            </a>
        </div>
        @if (Model == null || !Model.Any())
        {
            <div class="text-center text-gray-500 text-lg italic">Hiện chưa có phòng nào</div>
        }
        else
        {
            <table class="table-auto w-full border border-gray-300 text-center">
                <thead class="bg-red-500 text-white">
                    <tr>
                        <th class="py-3 px-4 border">Mã Phòng Học</th>
                        <th class="py-3 px-4 border">Tên Phòng Học</th>
                        <th class="py-3 px-4 border">Sức Chứa</th>
                        <th class="py-3 px-4 border" colspan="2">Hành Động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var phong in Model)
                    {                     
                        <tr class="hover:bg-gray-100">
                            <td class="py-3 px-4 border">@phong.IDPhongHoc</td>
                            <td class="py-3 px-4 border">@phong.TenPhong</td>
                            <td class="py-3 px-4 border">@phong.SucChua</td>
                            <td class="py-3 px-4 border">
                                <a href="@Url.Action("Xem", "QuanLyPhongHoc", new { id = phong.IDPhongHoc })"
                                   class="inline-block py-2 px-4 text-sm font-medium rounded-lg bg-gradient-to-r from-red-600 via-red-500 to-red-600
                                          bg-[length:200%_auto] bg-left hover:bg-right transition-all duration-1000 ease-out
                                          text-white shadow">
                                    Xem
                                </a>
                            </td>
                            <td class="py-3 px-4 border">
                                <a href="@Url.Action("Xoa", "QuanLyPhongHoc", new { id = phong.IDPhongHoc })"
                                   class="inline-block py-2 px-4 text-sm font-medium rounded-lg bg-gradient-to-r from-red-600 via-red-500 to-red-600
                                          bg-[length:200%_auto] bg-left hover:bg-right transition-all duration-1000 ease-out
                                          text-white shadow">
                                    Xóa
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>         
        }
    </div>
</div>

<script src="~/Scripts/JSThongBao.js"></script>
@if (ViewBag.ThongBao != null)
{
    <script>
            // Gọi hàm showAlert từ file JSThongBao.js
            showAlert('@Html.Raw(ViewBag.ThongBao)');
    </script>
}